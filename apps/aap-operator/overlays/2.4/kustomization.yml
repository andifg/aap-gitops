apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- ../../base

patches:
  - path: patch-subscription-channel-version.yaml
    target:
      kind: Subscription
      name: aap-operator
